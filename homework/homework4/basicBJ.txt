           /*int playerCardValue = 0;
            int dealerCardValue = 0;

            Random rnd = new Random();
            playerCardValue += rnd.Next(1, 12);
            playerCardValue += rnd.Next(1, 12);
            if (playerCardValue > 21)
                playerCardValue -= 10;

            dealerCardValue += rnd.Next(1, 12);
            dealerCardValue += rnd.Next(1, 12);
            if (dealerCardValue > 21)
                dealerCardValue -= 10;
            while (true)
            {
                if (playerCardValue == 21)
                {
                    Console.WriteLine("You're at 21");
                    break;
                }
                Console.WriteLine($"My card value is {playerCardValue}. Hit (1) or Stay (2)?");
                string answer = Console.ReadLine();
                if (answer == "1")
                {
                    playerCardValue += rnd.Next(1, 12);
                    if (playerCardValue > 21)
                    {
                        Console.WriteLine("Bust!");
                        break;
                    }
                    else
                        continue;
                }
                else if (answer == "2")
                {
                    break;
                }
                else
                {
                    Console.WriteLine("Incorrect input");
                    continue;
                }
            }
            Console.WriteLine($"Player's card value is {playerCardValue}");
            if (playerCardValue <= 21)
            {
                while (dealerCardValue < 21 && dealerCardValue < playerCardValue)
                {
                    dealerCardValue += rnd.Next(1, 12);
                }
                if (playerCardValue == dealerCardValue)
                {
                    Console.WriteLine("Player and dealer have tied.");
                }
                else if (playerCardValue < dealerCardValue && dealerCardValue <= 21)
                {
                    Console.WriteLine("Dealer has won.");
                }
                else if (dealerCardValue > 21)
                {
                    Console.WriteLine("Dealer busted. Player won.");
                }
                else if (dealerCardValue == 21)
                    Console.WriteLine("Dealer has won.");
            }
            else
            {
                Console.WriteLine("Dealer won. Player busted.");
            }